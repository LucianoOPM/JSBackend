<header>
  <nav class="navbar custom-navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
      <span class="navbar-brand">E-Commerce</span>
      <ul class="navbar-nav custom-nav-ul me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="#">Carrito</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Contacto</a>
        </li>
        {{! Si el usuario es admin, mostrar "agregar productos" y redirigir a la vista "realtimeproducts" }}
      </ul>
      <div class="me-auto mt-2">
        <form class="d-flex" role="search">
          <input
            class="form-control me-2"
            type="search"
            placeholder="Search Product"
            aria-label="Search"
          />
          <button class="btn btn-outline-success" type="submit">Search</button>
        </form>
      </div>
      <div>
        <a href="/login"><button
            class="btn btn-outline-success login-button"
          >Iniciar Sesion</button></a>{{! Si hay una sesion activa, cerrar sesion }}
        {{!-- <h3>Hola! {{first_name}} {{last_name}} ROL: {{rol}}</h3>  Si está logueado mostrar mensaje si está logueado y es admin mostrar el rol, si no, no mostrar nada--}}
      </div>
    </div>
  </nav>
</header>

<main>
  {{#each docs}}
    <div class="card mb-3 mt-3 products" style="max-width: 750px;">
      <div class="row g-0">
        <div class="col-md-4 mt-5">
          <img
            src="{{this.thumbnail}}"
            class="img-fluid rounded-start"
            alt="{{this.category}}"
          />
        </div>
        <div class="col-md-8">
          <div class="card-body">
            <h5 class="card-title">Producto:
              <a href="/products/{{this.id}}">{{this.title}}</a></h5>
            <p class="card-text">Description: {{this.description}}</p>
            <p class="card-text">Price: {{this.price}}</p>
            <p class="card-text"><small class="text-body-secondary">Disponibles:
                {{this.stock}}</small></p>
            <input
              type="number"
              name="quantity"
              id="qty-{{this.code}}"
              min="1"
              max="{{this.stock}}"
            />
            <button class="btn btn-outline-success">Comprar</button>
          </div>
        </div>
      </div>
    </div>

  {{/each}}
  <nav aria-label="Page navigation example">
    <ul class="pagination justify-content-center">
      <li class="page-item {{!-- disabled --}}">
        <a
          class="page-link"
          href="{{prevLink}}"
          tabindex="-1"
          aria-disabled="true"
        >Previous</a>
      </li>
      <li class="page-item">
        <a class="page-link" href="{{nextLink}}">Next</a>
      </li>
    </ul>
  </nav>
</main>